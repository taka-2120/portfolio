import { services } from '@/constants/services';

export const app = services.find((service) => service.appName === "Eco Notify");
export const privacyPolicyURL = './' + app?.appName.toLowerCase().replace(/ /g, '') + '/privacy-policy';

# {app?.appName}

## {app?.heroEn}

{app?.iconSrc && <img src={app.iconSrc} alt={app.appName} style={{width: "160px", height: "160px", borderRadius: "40px", padding: '15px'}} />}

{<p style={{padding: '15px'}}>{app?.descriptionEn}</p>}

{app?.techStack && app.techStack.length !== 0 && <p style={{padding: '15px'}}><strong>Tech Stack</strong>: {app.techStack.join(', ')}</p>}

<a style={{padding: '15px', textDecoration: 'underline'}} href={privacyPolicyURL}>Privacy Policy</a>
